This directory should contain JSON configuration files for your gallery.

These files are generated by running:
1. python generate_descriptions.py (creates JSON from workflow outputs)
2. python generate_alt_text.py (adds alt_text fields)

Each JSON file should have the format:
{
  "workflow_name": "wf_name_provider_model_prompt_timestamp",
  "provider": "claude",
  "model": "claude-sonnet-4",
  "prompt_style": "narrative",
  "images": {
    "IMG_1234.jpg": {
      "description": "Full description text",
      "alt_text": "Short alt text for accessibility"
    }
  }
}
